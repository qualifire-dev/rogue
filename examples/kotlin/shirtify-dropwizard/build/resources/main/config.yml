server:
  applicationConnectors:
    - type: http
      port: 10004
  adminConnectors:
    - type: http
      port: 10005

openaiApiKey: ${OPENAI_API_KEY}
openaiModel: gpt-4o
agentName: "Shirtify T-Shirt Store Agent (Dropwizard)"

logging:
  level: INFO
  appenders:
    - type: console
